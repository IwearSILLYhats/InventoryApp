extends layout 

block content
    h1= title

    ul
        each supply in supplyList
            li 
                a(href=supply.url) #{supply.name}

    a(href="supply/create")
        p Create a new supply